(function(){const modal=document.getElementById('videoModal'),panel=document.getElementById('modalPanel'),video=document.getElementById('modalVideo'),title=document.getElementById('modalTitle');document.querySelectorAll('.open-video').forEach(b=>b.addEventListener('click',()=>{if(!modal)return;title.textContent=b.dataset.title||'Video';const ratio=b.dataset.ratio||'landscape';video.className=`modal-video ${ratio}`;panel.className='modal-panel';if(ratio==='portrait')panel.classList.add('portrait');video.innerHTML=`<iframe src="${b.dataset.video}" allow="autoplay; fullscreen" allowfullscreen></iframe>`;modal.classList.add('show');document.body.style.overflow='hidden'}));function close(){if(!modal)return;modal.classList.remove('show');document.body.style.overflow='';if(video)video.innerHTML=''}document.getElementById('closeModal')?.addEventListener('click',close);modal?.addEventListener('click',e=>{if(e.target===modal)close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()});const buttons=[...document.querySelectorAll('.tab-btn')],panels=[...document.querySelectorAll('.tab-panel')];function setTab(t){buttons.forEach(b=>b.classList.toggle('active',b.dataset.tab===t));panels.forEach(p=>p.classList.toggle('active',p.id===t));const u=new URL(location.href);u.searchParams.set('tab',t);history.replaceState(null,'',u)}buttons.forEach(b=>b.addEventListener('click',()=>setTab(b.dataset.tab)));const q=new URLSearchParams(location.search).get('tab');if(q&&document.getElementById(q))setTab(q)})();